<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.blackrabbit.kis.KISMapper">

  <!-- 유효기간 1일 토큰 발급 및 저장 -->
  <insert id="upsertKisToken" parameterType="KISTokenResDTO">
    INSERT INTO HC_api_token (
      user_id, access_token, token_type, expires_at, updated_at
    ) VALUES (
      #{userId}, #{access_token}, #{token_type}, #{access_token_token_expired}, NOW()
    )
    ON DUPLICATE KEY UPDATE
      access_token = VALUES(access_token),
      token_type = VALUES(token_type),
      expires_at = VALUES(expires_at),
      updated_at = NOW()
  </insert>


  <!-- db에 저장해둔 appkey 가져오기 -->
  <select id="getKisApiKey" parameterType="KISTokenDTO" resultType="KISTokenDTO">
    SELECT encrypted_app_secret, mock_account_no, encrypted_app_key
    FROM HC_user
    WHERE 1=1
    AND username = #{username}
  </select>
</mapper>